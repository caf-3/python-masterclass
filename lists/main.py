# list = is used to store multiple items in a single variable (similar to Array)

school_subjects = ["Matematica", "Português", "Quimica", "Filosofia"]

# school_subjects.append("Geografia")
# school_subjects.remove("Quimica")
# school_subjects.pop()
# school_subjects.insert(1, "Biologia")
# school_subjects.sort()
# school_subjects.clear()

for subject in school_subjects:
    print(subject)